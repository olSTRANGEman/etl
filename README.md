Много не отсортированной даты

Требования по библиотекам и сделаны машинно, поэтому не уверен, но через conda вроде стыкуется(желательно делать через env). 


Примеры запусков и обработки:


corset - создание подвыборки датасета, без аргументов. Переписывается в __main__

python3 sdv_test --dataset *some data*  создание синт массива, но почему то сейчас генерируются не качественные данные. Переделан из CGAN в VAE и работает в статическом режиме(аргумент не используется)
 

python3 train.py  --train_file *./creditcard_subset.csv* --test_file *creditcard.csv* здесь обучается на синт. подвыборке и запускается на изначальном 

